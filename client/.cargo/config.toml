[profile.release]
lto = "fat"
opt-level = 'z'
codegen-units = 1

# These 2 make the br compressed wasm a bit smaller.
# Using strip = "symbols" aka true breaks the client.
strip = "debuginfo"
panic = "abort"

